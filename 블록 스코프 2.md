#include <iostream>
using namespace std;
int main()
{
	if (int num = 1)
	{
		cout << num << endl;
	}

	//num = 1;  접근할 수 없음
	
	if (int num = 0)
	{
		cout << num << endl;
	}
	else
	{
		cout << num << endl; // else에서는 접근할 수 있음
	}
	
	for (int i = 0; i < 10; ++i)
	{
	
	}
	// i = 1; // 접근할 수 없음
	return 0;
}